apiVersion: v1
kind: Service
metadata:
  name: {{ include "t24-deploy.fullname" . }}
  labels:
    {{- include "t24-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "t24-deploy.selectorLabels" . | nindent 4 }}
