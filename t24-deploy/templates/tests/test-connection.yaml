apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "t24-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "t24-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "t24-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
